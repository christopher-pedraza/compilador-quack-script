// Prueba: Función con parámetros
program TestParams;
var res_suma: int;
const pi: float = 3.14;
var radius: float;

int sum(a: float, b: float) [
    var result: int = a + b;
    {
        return result;
    }
];

float get_area(radius: float) [{
    return (radius * radius * pi);
}];

main {
    //res_suma = sum(3.2, 4.5);
    //print(res_suma);
    print(get_area(2));
}
end