// Prueba: Instrucción print con varias expresiones y cadenas
program TestPrint;
var num1: int = 25;

int suma(a: int, b: int) [
    {
        return a+b;
    }
];

main {
    print("exp_plus: ", num1 + suma(2, 3));
    print("\nexp_minus: ", (1E2 / 10) - 25);
}
end