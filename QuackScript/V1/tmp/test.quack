// Prueba: Función con parámetros
program TestParams;
var res_suma: int;

int sum(a: int, b: int) [
    var result: int = a + b;
    {
        return result;
    }
];

main {
    res_suma = sum(3, 4);
    print(res_suma);
}
end